{% extends "base.jinja" %}

{% block content %}
    <h3>Create Account</h3>
    <p>
        {{ form.name.label }}<br>
        {{ form.name(size=32) }} 
    </p>
    <p>
        {{ form.email.label }}<br>
        {{ form.email(size=32) }}
    </p>
    <p>
        {{ form.password.label }}<br>
        {{ form.password(size=32) }}
    </p>

{% endblock content %}