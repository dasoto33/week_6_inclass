{% extends "base.jinja" %}

{% block content %}

  <p>Sean says {{ instructors['sean'][0] }}</p>
  <p>Dylan says {{ instructors.dylan[0] }}</p>

  {% for student in students %}
    <p>{{ student.title() }} says {{ students[student][0] }}</p>
  {% endfor %}

{% endblock content %}